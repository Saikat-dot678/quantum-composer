# FastAPI server configuration. Copy to your process manager's secret store or
# local untracked environment; never commit a populated .env file.
IBM_QUANTUM_API_KEY=
IBM_QUANTUM_INSTANCE=
IBM_QUANTUM_CHANNEL=ibm_quantum_platform

# Additional comma-separated browser origins for deployed frontends.
QUANTUM_COMPOSER_CORS_ORIGINS=
