fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
qiskit>=1.2,<3.0
qiskit-aer>=0.15,<1.0
pytest>=8.3,<9.0
httpx>=0.27,<1.0

# Optional: enables the very fast, large-scale Clifford/stabilizer engine.
# The backend runs fine without it (the engine reports itself as unavailable and
# the router falls back to Aer's 'stabilizer' method). Install with:
#   pip install stim
# stim>=1.13,<2.0
