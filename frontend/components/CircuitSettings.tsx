interface Props { qubits: number; clbits: number; columns: number; shots: number; onQubitsChange: (v: number) => void; onClbitsChange: (v: number) => void; onColumnsChange: (v: number) => void; onShotsChange: (v: number) => void }
function Stepper({ label, value, min, max, onChange }: { label: string; value: number; min: number; max: number; onChange: (v: number) => void }) { return <div className="flex items-center justify-between rounded-lg border border-slate-200 px-3 py-2"><span className="text-xs text-slate-500">{label}</span><div className="flex items-center gap-2"><button type="button" onClick={() => onChange(Math.max(min, value - 1))} disabled={value <= min} className="h-6 w-6 rounded bg-slate-100 text-slate-600 disabled:opacity-30">−</button><span className="w-5 text-center font-mono text-xs font-semibold">{value}</span><button type="button" onClick={() => onChange(Math.min(max, value + 1))} disabled={value >= max} className="h-6 w-6 rounded bg-slate-100 text-slate-600 disabled:opacity-30">+</button></div></div>; }
export function CircuitSettings(props: Props) { return <section><h2 className="mb-3 text-xs font-semibold uppercase tracking-[.16em] text-slate-500">Circuit settings</h2><div className="space-y-2"><Stepper label="Qubits" value={props.qubits} min={1} max={8} onChange={props.onQubitsChange} /><Stepper label="Classical bits" value={props.clbits} min={0} max={8} onChange={props.onClbitsChange} /><Stepper label="Time steps" value={props.columns} min={4} max={20} onChange={props.onColumnsChange} /><label className="block rounded-lg border border-slate-200 px-3 py-2 text-xs text-slate-500">Shots<select value={props.shots} onChange={(e) => props.onShotsChange(Number(e.target.value))} className="mt-1 w-full bg-white py-1 font-mono font-semibold text-slate-800 outline-none">{[128,256,512,1024,2048,4096,8192].map((v) => <option key={v}>{v}</option>)}</select></label></div></section>; }
