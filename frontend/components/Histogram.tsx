interface Props { counts: Record<string, number> }
export function Histogram({ counts }: Props) { const entries = Object.entries(counts).sort(([a], [b]) => a.localeCompare(b)); const max = Math.max(1, ...entries.map(([, c]) => c)); if (!entries.length) return <div className="flex h-40 items-center justify-center text-sm text-slate-400">Run a circuit to see outcome probabilities.</div>; return <div className="flex h-52 items-end gap-3 overflow-x-auto border-b border-l border-slate-200 px-4 pt-4">{entries.map(([state, count]) => <div key={state} className="flex h-full min-w-12 flex-1 flex-col items-center justify-end"><span className="mb-1 font-mono text-[10px] text-slate-500">{count}</span><div className="w-full max-w-16 rounded-t-md bg-gradient-to-t from-violet-600 to-cyan-400 transition-all" style={{ height: `${Math.max(3, count / max * 150)}px` }} /><span className="mt-2 font-mono text-xs font-semibold text-slate-700">{state}</span></div>)}</div>; }
