import type { Preset } from "@/lib/types";
interface Props { presets: Preset[]; onLoad: (preset: Preset) => void }
export function PresetCircuits({ presets, onLoad }: Props) { return <section className="mt-7 border-t border-slate-200 pt-6"><h2 className="mb-3 text-xs font-semibold uppercase tracking-[.16em] text-slate-500">Teaching presets</h2><div className="space-y-2">{presets.map((preset) => <button key={preset.id} type="button" onClick={() => onLoad(preset)} title={preset.description} className="group w-full rounded-lg border border-transparent px-3 py-2.5 text-left transition hover:border-slate-200 hover:bg-white hover:shadow-sm"><span className="block text-sm font-medium text-slate-700 group-hover:text-violet-700">{preset.name}</span><span className="mt-0.5 block text-[11px] leading-4 text-slate-400">{preset.description}</span></button>)}</div></section>; }
