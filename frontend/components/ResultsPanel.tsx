import type { SimulationResult } from "@/lib/types";
import { Histogram } from "./Histogram";
interface Props { result: SimulationResult | null; running: boolean }
export function ResultsPanel({ result, running }: Props) { const total = result ? Object.values(result.counts).reduce((sum, v) => sum + v, 0) : 0; return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-panel"><div className="mb-4 flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-slate-400">Local simulator</p><h2 className="mt-1 text-lg font-semibold text-slate-800">Measurement results</h2></div><div className="flex gap-2"><span className="rounded-lg bg-slate-100 px-3 py-2 text-xs text-slate-500">Depth <b className="ml-1 text-slate-800">{result?.depth ?? "—"}</b></span><span className="rounded-lg bg-slate-100 px-3 py-2 text-xs text-slate-500">Shots <b className="ml-1 text-slate-800">{total || "—"}</b></span></div></div>{running ? <div className="flex h-52 items-center justify-center"><span className="h-8 w-8 animate-spin rounded-full border-2 border-violet-200 border-t-violet-600" /></div> : <Histogram counts={result?.counts ?? {}} />}{result && <div className="mt-5 grid gap-4 lg:grid-cols-2"><div><h3 className="mb-2 text-xs font-semibold uppercase tracking-wider text-slate-400">Gate counts</h3><div className="flex flex-wrap gap-2">{Object.entries(result.gate_counts).map(([gate, count]) => <span key={gate} className="rounded-full bg-violet-50 px-3 py-1 font-mono text-xs text-violet-700">{gate.toUpperCase()} · {count}</span>)}</div></div><div><h3 className="mb-2 text-xs font-semibold uppercase tracking-wider text-slate-400">Circuit diagram</h3><pre className="max-h-44 overflow-auto rounded-lg bg-slate-950 p-3 text-[10px] text-slate-200">{result.diagram}</pre></div></div>}{result?.warnings.map((warning) => <p key={warning} className="mt-3 rounded-lg bg-amber-50 px-3 py-2 text-xs text-amber-700">{warning}</p>)}</section>; }
