import type { CircuitOperation, GateName } from "@/lib/types";
interface Props { operation?: CircuitOperation; connector?: CircuitOperation; qubit: number; selectedGate: GateName; pending: boolean; onClick: () => void }
function gateLabel(op: CircuitOperation, qubit: number) { if (op.gate === "cx") return op.qubits[0] === qubit ? "●" : "⊕"; if (op.gate === "cz") return op.qubits[0] === qubit ? "●" : "Z"; if (op.gate === "swap") return "×"; if (op.gate === "measure") return "M"; if (op.gate === "barrier") return "║"; return op.gate.toUpperCase(); }
export function GateCell({ operation, connector, qubit, selectedGate, pending, onClick }: Props) { const min = connector ? Math.min(...connector.qubits) : -1; const max = connector ? Math.max(...connector.qubits) : -1; return <button type="button" onClick={onClick} title={operation ? `Remove ${operation.gate.toUpperCase()}` : `Place ${selectedGate.toUpperCase()} on q${qubit}`} className={`relative flex h-12 w-14 shrink-0 items-center justify-center ${pending ? "rounded-lg bg-violet-100 ring-2 ring-violet-500" : ""}`}><span className="absolute left-0 right-0 top-1/2 h-px bg-slate-300" />{connector && qubit >= min && qubit <= max && <span className={`absolute left-1/2 z-0 w-px bg-violet-500 ${qubit === min ? "top-1/2 bottom-0" : qubit === max ? "top-0 bottom-1/2" : "inset-y-0"}`} />}{operation && <span className={`relative z-10 flex h-9 min-w-9 items-center justify-center rounded-lg border px-1 text-xs font-bold shadow-sm ${operation.gate === "barrier" ? "border-dashed border-slate-400 bg-slate-100 text-slate-500" : operation.gate === "measure" ? "border-emerald-500 bg-emerald-50 text-emerald-700" : "border-violet-500 bg-white text-violet-700"}`}>{gateLabel(operation, qubit)}</span>}{!operation && !pending && <span className="relative z-10 h-2 w-2 rounded-full bg-white opacity-0 ring-1 ring-violet-300 transition hover:opacity-100" />}</button>; }
